#!/usr/bin/perl 
use CGI qw/:standard/;
use CGI; 
use strict;
use warnings;
 



my $cgi = CGI->new();

#read parameters
my $name = $cgi->param("name");
my $template = $cgi->param("template");
my $metadata = $cgi->param("metadata");




#git clone

#add.sh 

#give back the file
