

#	Baliscore 
#---------------------------
#requires a msf file format for both reference an test alignment
#Here Mview converts fasta to msf if needed. 
#
#Baliscore computes : 
#-SP score 
#-TC score


##convert FASTA alignment to MSF format
mview -in fasta -out msf aln.fa > aln.msf


##convert FASTA reference alignment to MSF format
[[ -f $datasets_home/${dataset_name}/${id}.fa.ref ]] && mview -in fasta -out msf \
$datasets_home/${dataset_name}/${id}.fa.ref > $datasets_home/${dataset_name}/${id}.msf.ref


##BALI SCORE
bali_score $datasets_home/${dataset_name}/${id}.msf.ref aln.msf | grep auto | awk '{ print "SP="\$3 ";TC="\$4 }' > score.out



